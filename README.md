# Cantaloupe docker-compose with Let's Encrypt

Forked from here: https://github.com/MITLibraries/docker-cantaloupe

### Instructions
- edit the docker-compose.yaml file and set your config options there. Any config properties for cantaloupe.properties can be passed in through the environment variables.
- run ```docker-compose up -d```
